name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  PYTEST_ADDOPTS: "--color=yes"
  MAMBA_ROOT_PREFIX: "${{ github.workspace }}/.micromamba"
  # 👇 Отключаем build isolation глобально для pip
  PIP_NO_BUILD_ISOLATION: "true"

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup micromamba
        uses: mamba-org/setup-micromamba@v2
        with:
          micromamba-version: "latest"
          init-shell: bash

      - name: Cache conda environment
        uses: actions/cache@v4
        id: cache-conda
        with:
          path: ${{ env.MAMBA_ROOT_PREFIX }}/envs/ae
          key: ${{ runner.os }}-conda-ae-${{ hashFiles('conda-lock.yml') }}
          restore-keys: ${{ runner.os }}-conda-ae-

      - name: Install environment from conda-lock
        if: steps.cache-conda.outputs.cache-hit != 'true'
        run: micromamba create -n ae --file conda-lock.yml

      - name: Install local package
        run: |
          micromamba activate ae
          pip install --no-deps --no-build-isolation -e ".[development]"

      - name: Run unit tests
        run: pytest tests/unit -v -n auto --cov=src/ae --cov-report=xml --cov-report=term-missing -m unit

      - name: Run integration tests
        run: pytest tests/integration -v -m "integration and not slow" --cov=src/ae --cov-report=xml --cov-report=term-missing --cov-append

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: tests
          name: codecov-ubuntu
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    needs: test
    defaults:
      run:
        shell: bash -l {0}

    steps:
      - uses: actions/checkout@v4

      - name: Setup micromamba
        uses: mamba-org/setup-micromamba@v2
        with:
          micromamba-version: "latest"
          init-shell: bash

      - name: Cache conda environment
        uses: actions/cache@v4
        id: cache-conda-lint
        with:
          path: ${{ env.MAMBA_ROOT_PREFIX }}/envs/ae
          key: ${{ runner.os }}-conda-ae-${{ hashFiles('conda-lock.yml') }}
          restore-keys: ${{ runner.os }}-conda-ae-

      - name: Install environment from conda-lock
        if: steps.cache-conda-lint.outputs.cache-hit != 'true'
        run: micromamba create -n ae --file conda-lock.yml

      - name: Install local package & lint tools
        run: |
          micromamba activate ae
          pip install --no-deps --no-build-isolation -e ".[development]"
          pip install flake8 mypy ruff

      - name: Run ruff check
        run: ruff check src/ae tests

      - name: Run flake8
        run: |
          flake8 src/ae tests --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 src/ae tests --count --exit-zero --max-complexity=15 --max-line-length=127 --statistics

      - name: Run mypy
        run: mypy src/ae --ignore-missing-imports --no-strict-optional --no-error-summary

  build:
    name: Build & Install
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}

    steps:
      - uses: actions/checkout@v4

      - name: Setup micromamba
        uses: mamba-org/setup-micromamba@v2
        with:
          micromamba-version: "latest"
          init-shell: bash

      - name: Cache conda environment
        uses: actions/cache@v4
        id: cache-conda-build
        with:
          path: ${{ env.MAMBA_ROOT_PREFIX }}/envs/ae
          key: ${{ runner.os }}-conda-ae-${{ hashFiles('conda-lock.yml') }}
          restore-keys: ${{ runner.os }}-conda-ae-

      - name: Install environment from conda-lock
        if: steps.cache-conda-build.outputs.cache-hit != 'true'
        run: micromamba create -n ae --file conda-lock.yml

      - name: Install build dependencies
        run: |
          micromamba activate ae
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Check package with twine
        run: twine check dist/*

      - name: Install from wheel
        run: |
          micromamba activate ae
          pip install --no-deps --no-build-isolation dist/*.whl