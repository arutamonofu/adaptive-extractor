Locate the target figure on the rendered PDF page image.

Target metadata:
{{TARGET_JSON}}

Return the bbox for the whole figure, not an individual panel. Include all panels, axes, legends, labels, and visual content belonging to the figure. Do not include the caption unless it is visually inseparable from the figure.

Do not locate, isolate, or crop individual panels.

Use normalized image coordinates [x0, y0, x1, y1], floats from 0 to 1.

If the target figure is not visible on the page, return "bbox_norm": null and add a warning.

Return strict JSON only:
{
  "target_id": "target_0001",
  "bbox_norm": [0.1, 0.2, 0.9, 0.8],
  "warnings": []
}
